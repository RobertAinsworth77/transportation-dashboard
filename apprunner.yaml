version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install
      - npm install -g serve
      - npm run build
run:
  runtime-version: '18'
  command: serve -s build -l 8080
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
